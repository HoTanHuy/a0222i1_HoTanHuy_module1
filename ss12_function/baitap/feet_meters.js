function feetToMeters(feet) {
    meters = 0.305 * feet;
    document.write(feet + " feet = "+ meters +" meters"+"<br>");
}
function metersToFeet(meters) {
    feet = 3.279 * meters;
    document.write(meters +" meters = "+feet + " feet" + "<br>" );
}